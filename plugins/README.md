# 插件目录

阶段 6 增加了可验证的知识库入口和 [内容过滤器模板](templates/content-filter/README.md)。知识库只绑定方案修订、哈希、样本证据和兼容性矩阵；外部 DLL 被视为受信任本机代码，当前版本不会自动扫描、发现或加载它们。

这里用于 KiriScope 的可选扩展和示例，不存放来历不明的二进制插件。

计划支持：

- XP3 结构变体
- 内容过滤器与密钥派生
- 作品方案
- 资源解码器
- 分析器
- 外部工具适配器

内置实现与外部插件共享能力模型，但作品方案应尽量只保存算法组合、参数、适用版本和验证证据。
