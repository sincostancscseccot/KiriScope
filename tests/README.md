# 测试目录

KiriScope 的首要质量目标是防止错误解密结果被标记为成功。

后续测试分为：

- `unit`：解析器、过滤器、路径、校验和格式验证。
- `integration`：组件、插件、CLI和外部工具适配。
- `regression`：真实缺陷裁剪出的合法最小样本。
- `malformed`：截断、越界、压缩异常和路径攻击输入。
- `end-to-end`：从扫描、提取、解密、转换到报告的完整流程。

测试数据必须可以合法保存在项目中；商业游戏完整资源和无法再分发的内容不得提交。
