# 架构文档
本目录将在正式开发后保存组件与协议的详细设计，包括：

- 项目清单、证据状态和产物来源模型。
- XP3 读取器与内容过滤器之间的边界。
- GUI、CLI与应用服务的数据流。
- 插件发现、能力声明、配置和兼容性协议。
- x86/x64运行时辅助进程的隔离与通信协议。
- 外部工具调用、版本探测和结果导入规范。

架构设计遵循依赖方向：前端依赖应用服务，应用服务依赖核心抽象；格式解析、插件和外部适配不能反向依赖 GUI。
