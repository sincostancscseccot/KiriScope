# 测试样本

本目录只保存自行构造、明确允许再分发或以元数据形式描述的最小样本。

计划分类：

- `synthetic`：由测试生成器构造的 XP3 与资源。
- `public`：许可证允许保存的公开样本。
- `manifests`：不能提交原文件时使用的哈希、结构与预期结果描述。

本机游戏样本、完整归档、提取艺术资源和进程转储必须放在被 `.gitignore` 排除的工作目录中。
